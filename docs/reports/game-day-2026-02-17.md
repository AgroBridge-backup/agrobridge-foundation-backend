# Game Day Report (2026-02-17)

Owner: Backend Platform Reliability

## Scope

Planned scenarios:

1. DB down
2. Redis down
3. Stripe degraded/errors
4. Error budget breach and override governance drill

## Execution Status

Executed on 2026-02-17:

- Scenario 4 completed with measured outcomes and artifact evidence.

Not executed in live preprod on 2026-02-17 (still blocked by missing environment access):

- `PREPROD_BASE_URL` unset
- `PREPROD_DATABASE_URL` unset
- `PROMETHEUS_BASE_URL` unset

## Executed Scenario Log

### Scenario 4: Error Budget Breach and Override Governance Drill

Goal:

- Verify fail-closed block on policy breach.
- Verify override evidence captures actor/run metadata.

Execution:

1. Ran `scripts/reliability/check-error-budget.mjs` with breaching synthetic signal and no override.
2. Confirmed hard block (non-zero exit).
3. Re-ran with `ERROR_BUDGET_OVERRIDE=approved` and explicit reason.
4. Confirmed pass-with-override and evidence artifact output.

Measured outcomes:

- MTTD (policy breach detection): `47ms`
- MTTR (decision + approved override execution): `52ms`
- Decision timeline:
  - T+0ms: breach check executed.
  - T+47ms: release blocked by policy.
  - T+52ms (second run): override-approved decision recorded.

Evidence:

- `/Users/mac/Documents/agrobridge-foundation-backend/artifacts/reliability/error-budget-check.json`
  - Includes `result: "override-approved"`, `runId`, `actor`, thresholds, and breach details.

## Pending Live Preprod Scenarios

### Scenario A: DB Down

- Detection time: pending live preprod access
- Runbook: `/Users/mac/Documents/agrobridge-foundation-backend/docs/runbooks/db-outage.md`

### Scenario B: Redis Down

- Detection time: pending live preprod access
- Runbook: `/Users/mac/Documents/agrobridge-foundation-backend/docs/runbooks/redis-outage.md`

### Scenario C: Stripe Degraded

- Detection time: pending live preprod access
- Runbook: `/Users/mac/Documents/agrobridge-foundation-backend/docs/runbooks/stripe-degraded.md`

## Corrective Actions Completed

1. Removed self-attested approver control path from deployment workflows.
2. Added protected environment lane expectation (`preprod` / `canary`) and override metadata evidence fields.
3. Enforced finite numeric validation for policy thresholds.
4. Added branch protection evidence artifact path for governance attribution.

## Remaining Gaps

1. Run one full live preprod drill once required secrets/access are available.
2. Capture DB/Redis/Stripe scenario MTTD/MTTR from real telemetry and append to this report.
3. Upgrade repository plan/features to enable GitHub Environment required-reviewer protection (current API returns HTTP 422 for reviewer/wait-timer rules).
